---
description: 文档编写与更新策略。仅在用户明确要求时修改 README、docs/ 等说明文档。
alwaysApply: true
---

# 文档策略

## 何时更新文档

- **默认不主动**创建或修改 `README.md`、`docs/**` 及其他面向用户的说明文档。
- 仅在用户**明确要求**更新文档时动手（例如「写进 README」「更新 CLIENT.md」）。
- 用户可感知的功能、修复或改进完成后，主动按 changelog 规则更新 `CHANGELOG.md`。
- 其他代码变更本身不构成更新说明文档的理由；任务完成说明写在对话中即可。

## 文档文风

- 使用简短、中性的技术说明；写命令、路径、配置项，不写操作引导式口语。
- 避免 AI 常见表述，例如：「推荐」「一键」「详见」「无需…即可」「内部团队可…」等。
- 不写图标生成步骤、镜像安装背景、Agent 工作流等与任务无关的维护说明，除非用户点名要写。

## 例外

- 用户可感知的代码变更需同步更新 `CHANGELOG.md`。
- 用户消息中直接包含文档修改需求。
- 用户提供的文档 diff 或明确文案需同步到仓库。
